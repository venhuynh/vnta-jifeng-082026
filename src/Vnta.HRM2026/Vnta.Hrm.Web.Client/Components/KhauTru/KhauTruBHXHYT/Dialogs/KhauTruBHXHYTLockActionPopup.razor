@namespace Vnta.Hrm.Web.Client.Components.KhauTru.KhauTruBHXHYT

<DxPopup Visible="@Visible"
         VisibleChanged="OnVisibleChangedAsync"
         HeaderText="@Title"
         Width="min(36rem, calc(100vw - 2rem))"
         ShowCloseButton="@(!IsBusy)"
         ShowFooter="true"
         CloseOnEscape="@(!IsBusy)"
         CloseOnOutsideClick="false"
         FooterCssClass="insurance-deduction-popup-footer"
         CssClass="insurance-deduction-lock-action-popup">
    <BodyContentTemplate>
        <div class="insurance-deduction-lock-action-content">
            <div class="insurance-deduction-confirm-message">@PromptText</div>
            <div class="insurance-deduction-lock-action-context">@ContextText</div>
            <div class="insurance-deduction-lock-action-options">
                <button type="button"
                        class="@GetScopeCssClass(SelectedRowsScope)"
                        @onclick="@(() => SelectScopeAsync(SelectedRowsScope))"
                        disabled="@(!CanChooseSelectedRowsScope || IsBusy)">
                    <span class="insurance-deduction-lock-action-title">Các dòng đã chọn</span>
                    <span class="insurance-deduction-lock-action-description">@SelectedRowsDescription</span>
                </button>
                <button type="button"
                        class="@GetScopeCssClass(WholePeriodScope)"
                        @onclick="@(() => SelectScopeAsync(WholePeriodScope))"
                        disabled="@IsBusy">
                    <span class="insurance-deduction-lock-action-title">Toàn bộ kỳ @WholePeriodLabel</span>
                    <span class="insurance-deduction-lock-action-description">@WholePeriodDescription</span>
                </button>
            </div>
        </div>
    </BodyContentTemplate>
    <FooterContentTemplate>
        <div class="insurance-deduction-confirm-actions">
            <DxButton Text="Hủy"
                      Click="CloseAsync"
                      IconUrl="@VntaDevExpressIcons.Cancel"
                      CssClass="insurance-deduction-popup-action"
                      Enabled="@(!IsBusy)"
                      RenderStyle="ButtonRenderStyle.Secondary"
                      RenderStyleMode="ButtonRenderStyleMode.Outline" />
            <DxButton Text="@(ShouldLock ? "Khóa" : "Mở khóa")"
                      Click="ConfirmAsync"
                      IconUrl="@(ShouldLock ? VntaDevExpressIcons.Lock : VntaDevExpressIcons.Unlock)"
                      CssClass="insurance-deduction-popup-action"
                      Enabled="@CanConfirm"
                      RenderStyle="ButtonRenderStyle.Primary"
                      RenderStyleMode="ButtonRenderStyleMode.Contained" />
        </div>
    </FooterContentTemplate>
</DxPopup>
