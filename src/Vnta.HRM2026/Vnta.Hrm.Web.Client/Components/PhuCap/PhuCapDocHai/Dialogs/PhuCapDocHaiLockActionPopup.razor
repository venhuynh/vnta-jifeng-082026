<DxPopup Visible="@Visible"
         VisibleChanged="OnVisibleChangedAsync"
         HeaderText="@Title"
         Width="min(36rem, calc(100vw - 2rem))"
         ShowCloseButton="@(!IsRefreshing)"
         ShowFooter="true"
         CloseOnEscape="true"
         CloseOnOutsideClick="false"
         FooterCssClass="hazard-popup-footer"
         CssClass="hazard-confirm-popup hazard-lock-action-popup">
    <BodyContentTemplate>
        <div class="hazard-lock-action-content">
            <div class="hazard-confirm-message">@PromptText</div>
            <div class="hazard-lock-action-context">@ContextText</div>
            <div class="hazard-lock-action-options">
                <button type="button"
                        class="@GetScopeCssClass(SelectedRowsScope)"
                        @onclick="@(() => SelectScopeAsync(SelectedRowsScope))"
                        disabled="@(!CanChooseSelectedRowsScope)">
                    <span class="hazard-lock-action-title">Các dòng đã chọn</span>
                    <span class="hazard-lock-action-description">@SelectedRowsDescription</span>
                </button>
                <button type="button"
                        class="@GetScopeCssClass(WholePeriodScope)"
                        @onclick="@(() => SelectScopeAsync(WholePeriodScope))">
                    <span class="hazard-lock-action-title">Toàn bộ kỳ @WholePeriodLabel</span>
                    <span class="hazard-lock-action-description">@WholePeriodDescription</span>
                </button>
            </div>
        </div>
    </BodyContentTemplate>
    <FooterContentTemplate>
        <div class="hazard-confirm-actions">
            <DxButton Text="Hủy"
                      Click="CloseAsync"
                      IconUrl="@VntaDevExpressIcons.Cancel"
                      CssClass="hazard-popup-action"
                      Enabled="@(!IsRefreshing)"
                      RenderStyle="ButtonRenderStyle.Secondary"
                      RenderStyleMode="ButtonRenderStyleMode.Outline" />
            <DxButton Text="@(ShouldLock ? "Khóa" : "Mở khóa")"
                      Click="ConfirmAsync"
                      IconUrl="@(ShouldLock ? VntaDevExpressIcons.Lock : VntaDevExpressIcons.Unlock)"
                      CssClass="hazard-popup-action"
                      Enabled="@CanConfirm"
                      RenderStyle="ButtonRenderStyle.Primary"
                      RenderStyleMode="ButtonRenderStyleMode.Contained" />
        </div>
    </FooterContentTemplate>
</DxPopup>
